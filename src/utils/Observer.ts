import { EMPTY_FUNCTION } from '@constants/const';
class Observer {
    private listeners: Array<Function> = [];
    constructor () {

    }
    public subscribe (name: string, listener: Function = EMPTY_FUNCTION ) {
        
    }
    public notify () {

    }
}